import React from "react";

export default function HeroSection() {
  return (
    <section id="hero-section" className="scroll-section hero-section">
      {/* Intentionally empty – background canvas acts as hero */}
    </section>
  );
}
